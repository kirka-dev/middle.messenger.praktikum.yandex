<!DOCTYPE html>
html(lang="en")
    head
        title Messenger
        block links
            link(rel="stylesheet", href="./index.scss")
        block scripts
    body(class='app')
        block content
        script(type='module' src='./index.ts')
